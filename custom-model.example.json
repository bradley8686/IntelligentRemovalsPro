{
  "enabled": false,
  "format": "tensorflowjs_graph_model",
  "model_url": "./models/removals/model.json",
  "labels_url": "./models/removals/labels.json",
  "minimum_confidence": 0.45,
  "notes": "Train a custom removals object detector, export it as TensorFlow.js, place the files under models/removals, then update this config."
}
